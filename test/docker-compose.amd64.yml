services:
  mysql:
    platform: linux/amd64

  addons-sync:
    platform: linux/amd64

  migrate:
    platform: linux/amd64

  redmine:
    platform: linux/amd64
    ports:
    - "3000:3000"
    environment:
      AGILE_BOARD_PROFILE: ${AGILE_BOARD_PROFILE:-0}

  active-jobs:
    platform: linux/amd64

  memcached:
    platform: linux/amd64

  postfix:
    platform: linux/amd64

  hub:
    platform: linux/amd64
